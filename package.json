{
  "name": "renovate-approve-bot",
  "version": "1.0.0",
  "description": "",
  "author": "Daichi Kimura <dkimura@bdd.xyz>",
  "license": "ISC",
  "repository": "https://github.com/dkimura/renovate-approve-bot.git",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "probot": "7.0.1",
    "ramda": "0.25.0"
  },
  "devDependencies": {
    "jest": "23.4.0",
    "nodemon": "1.18.1",
    "smee-client": "1.0.2",
    "standard": "12.0.1"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
